<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/home" app:showAsAction="ifRoom" android:enabled="true"
        android:icon="@drawable/home_nav_sector"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/chat" app:showAsAction="ifRoom"
        android:icon="@drawable/chat_nav_sector"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/note" app:showAsAction="ifRoom"
        android:enabled="true" android:icon="@drawable/note_nav_sector"
        tools:ignore="MenuTitle" />
    <item android:id="@+id/my" app:showAsAction="ifRoom"
        android:enabled="true" android:icon="@drawable/my_nav_sector"
        tools:ignore="MenuTitle" />
</menu>